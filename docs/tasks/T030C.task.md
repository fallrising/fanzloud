---
id: T030C
title: SQLite snapshot cache load
status: blocked
milestone: P1
primary_module: event-store
contract_units: [CU-EVT-03]
depends_on: [T030D]
risk: medium
---

# Outcome

Load an absent or present versioned session snapshot cache through an E0 bounded-resource boundary.

# Inputs

TD §4.6, accepted T030A/T030D schema and snapshot type.

# Deliverables

`load_snapshot` with absent, present, stale, corrupt, restart, bound, and no-mutation evidence.

# Non-scope

Snapshot production, saving, event replay, reducer trust, repair, or deletion.

# Invariants

Load never mutates durable history, and a snapshot is never represented as source of truth.

# Dependency Evidence

Blocked until T030D resolves and accepts snapshot shape and persistence semantics.

# Decomposition

One E0 `CU-EVT-03` boundary.

# Machine Acceptance

Focused snapshot-load tests plus all workspace gates.

# Required Documentation Updates

Create `SPEC-T030C`, traceability, rustdoc, handoff, and `ACCEPT-T030C`.

# Risks and Gaps

No independent implementation can proceed until the T030D TD-GAP establishes the persisted
snapshot shape and failure visibility.
